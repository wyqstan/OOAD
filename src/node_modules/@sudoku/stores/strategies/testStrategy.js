import { SUDOKU_SIZE } from '@sudoku/constants'
//import { currentBoard, currentAnswerCandidates } from '@sudoku/stores/solver'
import { currentBoard, currentAnswerCandidates } from '@sudoku/stores/boardForSolve'

export function testStrategy(){
        currentBoard.subscribe((board) => {
            console.log(board[0][0]); // 使用解包后的 board 值
        });
    console.log('there is the test strategy.');

    return currentAnswerCandidates;
}